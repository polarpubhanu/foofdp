# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/foodRescue

# JWT Secret Key for Authentication
JWT_SECRET=your_super_secret_jwt_key_here

# NEXT_PUBLIC_ (if needed for client-side API calls, though Next.js API routes are typically internal)
# NEXT_PUBLIC_MAPBOX_API_KEY= (Not used, using Leaflet)
